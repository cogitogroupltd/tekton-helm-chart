apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: dev-
spec:
  pipelineRef:
    name: dev
  workspaces:
    - name: git-pvc
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 1Gi
    - name: ssh-creds
      secret:
        secretName: "dev-pipeline-ssh-key"
    - name: docker-creds-cm
      secret:
        secretName: "dev-pipeline-docker-config"
    - name: kubeconfig
      configMap:
        name: "dev-pipeline-kube-config"